# steps 1: download source code from github (https://github.com/Levinci-Harmony/hp-admin.git) branch production

# steps 2: reinstall the library
### `npm install`

# steps 3: In the project directory, you can run:
### `npm run start:w`
